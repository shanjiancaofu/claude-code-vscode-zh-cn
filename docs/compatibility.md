# 兼容性

当前翻译表基于 Claude Code VS Code `2.1.183` 测试。

目录检测支持 Windows、macOS、Linux、WSL、Remote SSH、Dev Container、VS Code Server、Cursor、Windsurf、VSCodium、code-server 和 OpenVSCode Server。非标准安装路径可用 `--target` 或设置项指定。

Claude Code 更新后可能增加或修改 UI 文案。工具会提示版本差异并统计实际可替换数量；替换数量为零时不会创建无意义备份或写入文件。
